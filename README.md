# Welcome to Actions Example!

...

## Usage

To build the main Docker image:

```bash
docker build -t actions_example:latest .
```

To run this image in a new container:

```bash
docker run -e MY_PASSWORD=1234 actions_example:latest
```
